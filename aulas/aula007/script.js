function verificar(){
    var data = new Date()
    var ano = data.getFullYear()
    var fano = window.document.querySelector('input#tano')
    var res = window.document.querySelector('div#res')
    if(fano.ariaValueMax.length == 0)
}